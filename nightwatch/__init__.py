"""nightwatch — the run rail for unattended coding-agent work.

A queue, a time window, two lids, two tiers, and a log that reads git instead of
reading its own record of itself. It does not decide what the agent may write —
that is curfew, a separate tool.
"""
__version__ = "0.1.0"
